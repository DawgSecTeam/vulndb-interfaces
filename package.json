{
  "name": "vulndb-ui",
  "version": "1.0.0",
  "description": "Catalog and editor for the scripted vulnerabilities, misconfigurations, and services used to provision vulnerable lab/range machines.",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "minio": "^8.0.7",
    "multer": "^2.2.0",
    "mysql2": "^3.22.3",
    "node-cron": "^3.0.3",
    "ssh2": "^1.17.0"
  }
}
